repositories {
    mavenCentral()
    google()

    // ADD THIS — the Road Runner 1.0 repo
    maven {
        url = uri("https://maven.pkg.github.com/acmerobotics/road-runner")
        credentials {
            username = "acmerobotics"  // GitHub username not required but allowed
            password = ""               // leave empty for read-only access
        }
    }
}
